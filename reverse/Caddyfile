# On Cloudflare, go to Crypto -> SSL and set it to Full or Full(strict)

{
	email info@watchit.movie
    debug
}


(shared_conf) {
    tls internal
    encode zstd gzip

	log {
		output stdout
    }
}


ipfs.watchit.movie {
    import shared_conf
    reverse_proxy  :8080   
    
}